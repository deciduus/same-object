---
name: C00-short-slug                 # must equal the filename without .md
type: computed                       # gap | move | method | theorem | computed | index | question | source
# --- optional, validated only when present ---
# exit: computation                  # prediction | computation | experiment | specification | none-yet
# extends-to: []                     # astrobiology | ecology | circularity | conservation | sustainability | none
# next-step-cost: M                  # S | M | L
---

# What the number is

> **The headline result in one sentence, with the number in it.** Which gap it narrows or
> closes, and what it does not do: [[G00-short-slug]].

## The quantity

```
X  ≡  ...
```

Definition, units, and the range it is bounded in (with the reason it is bounded).

## Inputs

Every input with its published source. Mark any number that is an estimate as UNVERIFIED.

## Result

The table or the value. Rows left empty because the data do not exist stay empty and say so.

## What it does not settle

The honest limit — the prefactor, the missing second axis, the case it is blind to.
